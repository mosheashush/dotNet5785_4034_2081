<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="VolunteerListWindow" Height="640" Width="800">
    <!--External Grid-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="23*"/>
            <RowDefinition Height="213*"/>
            <RowDefinition Height="76*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--Internal, Upper Grid-->
        <Grid HorizontalAlignment="Stretch" Height="auto" Grid.Row="0" VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Label" HorizontalAlignment="left" Margin="76,0,0,0" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </Grid>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Path=StudentList}" IsReadOnly ="True" AutoGenerateColumns="False"
  	 EnableRowVirtualization="True" RowDetailsVisibilityMode="VisibleWhenSelected"
  	 MouseDoubleClick="dgStudentList_MouseDoubleClick" SelectedItem="{Binding SelectedStudent}">
    <DataGrid.Columns>
        <DataGridTextColumn Binding="{Binding Id}" Header="Id" Width="Auto"/>
        <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="Auto"/>
        <DataGridTemplateColumn Header="Is Active" Width="Auto">
            <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                    <CheckBox IsEnabled="False" IsChecked="{Binding Path=IsActive}" />
                </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
  
    </DataGrid.Columns>
</DataGrid>

    </Grid>

</Window>
